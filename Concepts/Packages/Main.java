//importing packages
import operations.*;

//Main Class

public class Main{
    public static void main(String[] args) {
        //Creating objects for the class in packages
        Addition a=new Addition();
        Subraction s=new Subraction();
        a.add(4,5);
        s.sub(50,34);
    }
}